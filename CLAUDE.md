# Project Instructions

The instructions live in [`AGENTS.md`](AGENTS.md). This file is a pointer, not a copy —
duplicating them here would give the repository two answers to every question, and a copy
that can disagree with its original is the failure `AGENTS.md` itself warns about.

| Read | For |
|---|---|
| [`AGENTS.md`](AGENTS.md) | **Standing instructions.** What this project is, the source-of-truth chain, effort selection, single ownership and the extraction guard, verification, git and delivery. Start here. |
| [`agent.md`](agent.md) | **Lessons learned the hard way.** Retrospective notes — drift hazards, token economy, what has already gone wrong. |

Both apply regardless of which agent or tool is reading them.
