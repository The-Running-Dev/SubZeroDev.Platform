# unit/document/state-index
Kind: document
Status: active
Anchor: design/state-index.md
Consumes:
Exposes:
Binds:
Live:
Questions:
Work:
Evidence:

## Owns
The corpus-wide projections no single record can state alone: the unit table of contents and
the four reverse edges (`Invariant.BoundBy`, `Contract.Consumers`, `Decision.Affects`,
`Question.Affects`), plus the `Outstanding` WorkRef mirror. Every table in it is generated by
`tools/Update-DesignProjection.ps1` and never hand-edited.
